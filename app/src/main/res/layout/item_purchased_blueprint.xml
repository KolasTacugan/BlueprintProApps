<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/card_modern"
    android:layout_marginBottom="12dp">

    <!-- Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Icon Container -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/card_modern"
            android:backgroundTint="#E8F0FE"
            android:layout_marginEnd="16dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_menu_agenda"
                app:tint="#0D3C80" />
        </FrameLayout>

        <!-- Text Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <!-- Blueprint Name -->
            <TextView
                android:id="@+id/tvBlueprintName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Modern Bungalow Plan"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="#0D3C80"
                android:fontFamily="@font/raleway_bold"
                android:letterSpacing="0.01"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Architect Name -->
            <TextView
                android:id="@+id/tvArchitectName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Architect: Juan Dela Cruz"
                android:textSize="14sp"
                android:textColor="#6B7280"
                android:fontFamily="@font/raleway_regular"
                android:letterSpacing="0.01"
                android:layout_marginTop="4dp"
                android:maxLines="1"
                android:ellipsize="end" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>

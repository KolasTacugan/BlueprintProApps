<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".network.ArchitectBlueprintActivity">

    <!-- Use NestedScrollView only if RecyclerView is replaced with other scrollable content
         But since RecyclerView is scrollable itself, we don't need NestedScrollView -->

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Header -->
        <LinearLayout
            android:id="@+id/headerLayout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingTop="?attr/actionBarSize"
            android:padding="16dp"
            android:gravity="center_vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:elevation="4dp">

            <ImageButton
                android:id="@+id/backButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:minWidth="48dp"
                android:minHeight="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back"
                android:contentDescription="Back"
                android:padding="8dp"
                android:scaleType="centerInside"
                android:clickable="true"
                android:focusable="true"
                app:tint="#0D3C80" />


            <TextView
                android:id="@+id/titleText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="My Projects"
                android:textColor="#091635"
                android:textSize="20sp"
                android:textStyle="bold"
                android:layout_marginStart="16dp"/>
        </LinearLayout>

        <!-- RecyclerView for Blueprints -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewBlueprints"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:padding="16dp"
            android:clipToPadding="false"
            android:scrollbars="vertical"
            app:layout_constraintTop_toBottomOf="@id/headerLayout"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/item_archi_project_card" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- EXPAND FAB: More Options (3 dots) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/moreOptionsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="More Options"
        android:src="@drawable/ic_more_vert"
        app:backgroundTint="#0D3C80"
        app:tint="@android:color/white"
        app:layout_anchor="@id/recyclerViewBlueprints"
        app:layout_anchorGravity="bottom|end"
        android:layout_margin="24dp"/>

    <!-- Hidden: Add Project Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addProjectBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Add Project"
        android:src="@drawable/ic_add"
        app:backgroundTint="#0D3C80"
        app:tint="@android:color/white"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="84dp"
        android:visibility="gone"
        app:layout_anchor="@id/recyclerViewBlueprints"
        app:layout_anchorGravity="bottom|end"/>

    <!-- Hidden: Deleted Projects Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/deletedProjectsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Deleted Projects"
        android:src="@drawable/ic_delete"
        app:backgroundTint="#C62828"
        app:tint="@android:color/white"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="144dp"
        android:visibility="gone"
        app:layout_anchor="@id/recyclerViewBlueprints"
        app:layout_anchorGravity="bottom|end"/>


</androidx.coordinatorlayout.widget.CoordinatorLayout>
